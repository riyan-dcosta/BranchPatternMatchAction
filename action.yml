name: 'Hello World'
description: 'Greet input and record time'
inputs:
  who-to-greet:
    description: 'Who To Greet'
    required: true
    default: 'World'
outputs:
  time:
    description: 'The time we greeted you'
runs:
  using: 'node20'  
  main: 'src/branch-match.js'